#include "main.h"

/**
 * _abs - Evalute the absoulte value
 * @n: contain the value to be compared
 *
 * Return: Always 0.
 */
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
else
{
return ((-1) * n);
}
}
